{
  "name": "jefe-ops",
  "private": true,
  "type": "module",
  "description": "Jefe internal observability panel — a separate, gated ops app (never merchant-facing).",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
