# Neon project that holds waitlist_signups (same project apps/marketing writes to).
# Use a READ-ONLY credential for reporting. For prod, use the public proxy URL,
# not the internal host.
DATABASE_URL="postgresql://readonly_user:password@host.neon.tech/neondb?sslmode=require"
